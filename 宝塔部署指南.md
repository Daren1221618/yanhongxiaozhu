# 嫣红小主 · 宝塔面板部署指南

## 一、宝塔面板准备

### 1. 安装环境（软件商店）
- **Node.js 版本管理器** → 安装 Node 18+
- **PM2 管理器** → 安装
- Nginx 通常已安装

### 2. 创建网站
- 宝塔 → 网站 → 添加站点
- 域名：例如 `yanhong.yourdomain.com`（或子目录）
- PHP 版本：选择「纯静态」（我们不需要 PHP）
- 提交

---

## 二、上传项目

### 方式 A：通过宝塔文件管理器
1. 宝塔 → 文件 → 进入 `/www/wwwroot/` 
2. 新建目录 `yanhong-xiaozhu`
3. 上传以下文件/文件夹到 `/www/wwwroot/yanhong-xiaozhu/`：
   ```
   server.js
   package.json
   build.js
   public/        （整个文件夹）
   data/          （整个文件夹）
   railway.toml   （如果不需要 Railway 可以不上传）
   ```

### 方式 B：通过 Git（推荐）
1. SSH 连上服务器
2. `cd /www/wwwroot`
3. `git clone https://github.com/Daren1221618/yanhongxiaozhu.git yanhong-xiaozhu`
4. 此后更新只需 `cd /www/wwwroot/yanhong-xiaozhu && git pull`

---

## 三、安装依赖 & 启动

宝塔终端或 SSH 执行：

```bash
cd /www/wwwroot/yanhong-xiaozhu
npm install
```

### PM2 启动：

```bash
# 方式 1：直接启动
pm2 start server.js --name yanhong-xiaozhu

# 方式 2：用环境变量指定端口（避免与现有网站冲突）
PORT=3001 pm2 start server.js --name yanhong-xiaozhu

# 设置开机自启
pm2 save
pm2 startup
```

---

## 四、Nginx 反代配置

### 场景 A：独立域名/子域名

宝塔 → 网站 → 你的站点 → 设置 → 配置文件，替换为：

```nginx
server {
    listen 80;
    server_name yanhong.yourdomain.com;   # 改成你的域名

    location / {
        proxy_pass http://127.0.0.1:3001;  # 3001 改成 PM2 用的端口
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 场景 B：已有网站的子目录

如果现有网站是 `yourdomain.com`，想让 yanhong 走 `yourdomain.com/yanhong/`：

在现有站点配置的 server 块内添加：

```nginx
location /yanhong/ {
    proxy_pass http://127.0.0.1:3001/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

然后 server.js 启动时指定 PORT=3001。

修改后记得重载 Nginx：
```bash
nginx -s reload
# 或在宝塔面板点「重载配置」
```

---

## 五、验证

浏览器访问你配置的域名，应该能看到嫣红小主网站。

管理后台：`http://你的域名/admin/`
默认账号：`admin` / 密码：`yanhong2026`

---

## 六、后续更新

```bash
cd /www/wwwroot/yanhong-xiaozhu
git pull                    # 拉取最新代码
pm2 restart yanhong-xiaozhu # 重启服务
```

---

## 常见问题

**端口被占用：** 换一个端口，比如 3001、3002、8080 等。

**CSS/JS 加载不出来：** 检查 Nginx 配置中 proxy_pass 路径是否正确。

**管理后台无法登录：** 确保 `data/` 目录有写入权限：
```bash
chmod -R 755 /www/wwwroot/yanhong-xiaozhu/data
```
