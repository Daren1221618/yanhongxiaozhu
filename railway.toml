# 嫣红小主 · 花果茶鲜酿 — Railway 部署配置

[build]
  builder = "nixpacks"
  buildCommand = "echo '↷ 跳过构建，运行时生成数据'"

[deploy]
  numReplicas = 1

  [deploy.volumes]
    mountPath = "/app/data"

[service]
  healthcheckPath = "/api/check-auth"
